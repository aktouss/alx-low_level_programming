#include <stdio.h>

/**
     *main - Positive anything is better than negative nothing
     *Return: always (success)
     *
    */

int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	putchar(c);
	putchar('\n');
	return (0);
}

